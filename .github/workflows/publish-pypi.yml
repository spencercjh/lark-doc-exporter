name: publish-pypi

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  id-token: write

env:
  PYTHON_VERSION: "3.14"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@v8.2.0
        with:
          enable-cache: false

      - name: Sync dev dependencies
        run: uv sync --python ${{ env.PYTHON_VERSION }} --group dev

      - name: Refresh main branch for release boundary check
        run: git fetch origin +refs/heads/main:refs/remotes/origin/main

      - name: Require tagged commit on main
        run: |
          if ! git merge-base --is-ancestor "${GITHUB_SHA}" origin/main; then
            echo "::error::Tag ${GITHUB_REF_NAME} points to a commit outside origin/main. Refusing to publish."
            exit 1
          fi

      - name: Verify release tag and package versions
        run: uv run python -m lark_synced_export.release_version --tag "${GITHUB_REF_NAME}"

      - name: Run repository checks
        run: make ci

      - name: Rebuild release artifacts
        run: |
          rm -rf dist
          uv build

      - name: Smoke-test the built wheel
        run: uv tool run --isolated --from dist/lark_doc_exporter-*.whl lark-doc-exporter --help

      - name: Publish distribution to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
